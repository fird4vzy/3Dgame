# Copy this file to `.env` and paste your key in. `.env` is gitignored.
#
# Meshy: app.meshy.ai -> Settings -> API Keys -> Create new key.
# The key is shown once at creation; if you lost it, issue a new one.
#
# It looks like:  msy_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MESHY_API_KEY=
